# Base URL API quotation
API_BASE_URL=https://dev-api-quotation.motorsights.com/api/quotation

# SSO Auth — untuk generate Bearer token otomatis
AUTH_URL=https://dev-gateway.motorsights.com/api/auth/sso/login
AUTH_EMAIL=abdulharris@motorsights.net
AUTH_PASSWORD=your_password_here

# Timeout request dalam ms (opsional, default 30000)
API_TIMEOUT_MS=30000

# ---- HTTP MCP Server ----
MCP_HTTP_PORT=9533

# OAuth 2.0 PKCE untuk Claude.ai
# Set true untuk aktifkan, false untuk skip (development)
OAUTH_ENABLED=true
# URL publik server ini (dipakai di discovery metadata)
OAUTH_SERVER_BASE_URL=https://mcp-internal.motorsights.com
# client_id yang didaftarkan ke Claude.ai
OAUTH_CLIENT_ID=mcp-internal-client-a1b2c3d4
# client_secret (simpan aman, jangan commit ke git)
OAUTH_CLIENT_SECRET=your-client-secret-here
